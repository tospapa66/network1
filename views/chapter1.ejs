<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>บทที่ 1 - เครื่องคิดเลข</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;600&display=swap" rel="stylesheet">
    <style> body { font-family: 'Sarabun', sans-serif; } </style>
</head>
<body class="bg-gray-100 h-screen flex overflow-hidden">

    <aside class="bg-slate-800 text-white w-64 flex-shrink-0 flex flex-col hidden lg:flex">
        <div class="p-4 border-b border-slate-700 font-bold text-xl">Course Menu</div>
        <nav class="flex-1 overflow-y-auto py-4">
            <ul class="space-y-1">
                <li><a href="/" class="block px-4 py-2 hover:bg-slate-700 border-l-4 border-transparent">หน้าแรก</a></li>
                <% chapters.forEach((chapter, index) => { %>
                <li>
                    <a href="<%= chapter.link %>" class="block px-4 py-2 hover:bg-slate-700 border-l-4 <%= active === ('ch' + (index+1)) ? 'border-blue-500 bg-slate-700' : 'border-transparent' %> truncate">
                        <%= chapter.name %>
                    </a>
                </li>
                <% }); %>
            </ul>
        </nav>
    </aside>

    <div class="flex-1 flex flex-col h-screen overflow-hidden relative">
        <header class="bg-white shadow-sm p-4"><h1 class="text-2xl font-bold text-gray-800">บทที่ 1: Basic Operations (Workshop)</h1></header>

        <main class="flex-1 overflow-y-auto p-10 bg-slate-50">
            <div class="max-w-md mx-auto bg-white rounded-xl shadow-lg p-8">
                <h2 class="text-2xl font-bold mb-6 text-indigo-600 text-center">🧮 เครื่องคิดเลขเด็กวิทย์คอม</h2>
                
                <div class="space-y-4">
                    <input type="number" id="num1" placeholder="ตัวเลขที่ 1" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none text-lg">
                    <input type="number" id="num2" placeholder="ตัวเลขที่ 2" class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 outline-none text-lg">
                </div>

                <div class="grid grid-cols-4 gap-2 mt-6">
                    <button onclick="calc('+')" class="bg-blue-100 hover:bg-blue-200 text-blue-700 font-bold py-3 rounded text-xl">+</button>
                    <button onclick="calc('-')" class="bg-blue-100 hover:bg-blue-200 text-blue-700 font-bold py-3 rounded text-xl">-</button>
                    <button onclick="calc('*')" class="bg-blue-100 hover:bg-blue-200 text-blue-700 font-bold py-3 rounded text-xl">×</button>
                    <button onclick="calc('/')" class="bg-blue-100 hover:bg-blue-200 text-blue-700 font-bold py-3 rounded text-xl">÷</button>
                    <button onclick="calc('pow')" class="col-span-4 bg-indigo-500 hover:bg-indigo-600 text-white font-bold py-2 rounded mt-2">ยกกำลัง (Num1 ^ Num2)</button>
                </div>

                <div class="mt-8 p-4 bg-gray-100 rounded-lg text-center">
                    <span class="text-gray-500 text-sm">ผลลัพธ์</span>
                    <div id="result" class="text-4xl font-bold text-gray-800 mt-2">-</div>
                </div>
            </div>
        </main>
    </div>

    <script>
        function calc(op) {
            const n1 = parseFloat(document.getElementById('num1').value);
            const n2 = parseFloat(document.getElementById('num2').value);
            let res = 0;

            if (isNaN(n1) || isNaN(n2)) { alert("กรุณาใส่ตัวเลขทั้งสองช่อง"); return; }

            switch(op) {
                case '+': res = n1 + n2; break;
                case '-': res = n1 - n2; break;
                case '*': res = n1 * n2; break;
                case '/': res = n2 !== 0 ? n1 / n2 : 'Infinity'; break;
                case 'pow': res = Math.pow(n1, n2); break;
            }
            document.getElementById('result').innerText = res;
        }
    </script>
</body>
</html>